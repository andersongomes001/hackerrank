

        HashSet<String> sets = new HashSet<>();
        for (int i = 0; i < t; i++) {
            sets.add(String.format("%s %s", pair_left[i], pair_right[i]));
            System.out.println(sets.size());
        }

